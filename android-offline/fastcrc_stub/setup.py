from setuptools import setup

setup(
    name="fastcrc",
    version="0.3.6",
    description="Android compatibility stub for pymavlink; pymavlink falls back to its pure-Python CRC implementation.",
    packages=["fastcrc"],
)
